/**
 * Inventory API
 * The Inventory API is used to create and manage inventory, and then to publish and manage this inventory on an eBay marketplace. There are also methods in this API that will convert eligible, active eBay listings into the Inventory API model.
 *
 * The version of the OpenAPI document: 1.18.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Overrides from './Overrides';
import WeeklySchedule from './WeeklySchedule';

/**
 * The SameDayShippingCutOffTimes model module.
 * @module model/SameDayShippingCutOffTimes
 * @version 1.18.4
 */
class SameDayShippingCutOffTimes {
    /**
     * Constructs a new <code>SameDayShippingCutOffTimes</code>.
     * This type is used by the &lt;b&gt;createInventoryLocation&lt;/b&gt; call to specify cut-off time(s) for an inventory location, as well as any overrides for these times.
     * @alias module:model/SameDayShippingCutOffTimes
     */
    constructor() { 
        
        SameDayShippingCutOffTimes.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SameDayShippingCutOffTimes</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SameDayShippingCutOffTimes} obj Optional instance to populate.
     * @return {module:model/SameDayShippingCutOffTimes} The populated <code>SameDayShippingCutOffTimes</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SameDayShippingCutOffTimes();

            if (data.hasOwnProperty('overrides')) {
                obj['overrides'] = ApiClient.convertToType(data['overrides'], [Overrides]);
            }
            if (data.hasOwnProperty('weeklySchedule')) {
                obj['weeklySchedule'] = ApiClient.convertToType(data['weeklySchedule'], [WeeklySchedule]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SameDayShippingCutOffTimes</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SameDayShippingCutOffTimes</code>.
     */
    static validateJSON(data) {
        if (data['overrides']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['overrides'])) {
                throw new Error("Expected the field `overrides` to be an array in the JSON data but got " + data['overrides']);
            }
            // validate the optional field `overrides` (array)
            for (const item of data['overrides']) {
                Overrides.validateJSON(item);
            };
        }
        if (data['weeklySchedule']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['weeklySchedule'])) {
                throw new Error("Expected the field `weeklySchedule` to be an array in the JSON data but got " + data['weeklySchedule']);
            }
            // validate the optional field `weeklySchedule` (array)
            for (const item of data['weeklySchedule']) {
                WeeklySchedule.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * This container can be used to override the existing cut-off time(s), specified in the <b>weeklySchedule</b> container, for a specific date or date range.
 * @member {Array.<module:model/Overrides>} overrides
 */
SameDayShippingCutOffTimes.prototype['overrides'] = undefined;

/**
 * This container is used to specify the weekly schedule for shipping and handling cut-off times. A cut-off time is required for each business day that the fulfillment center operates. Any orders made after the specified <b>cutOffTime</b> on the specified day(s) of the week will be handled on the next day.
 * @member {Array.<module:model/WeeklySchedule>} weeklySchedule
 */
SameDayShippingCutOffTimes.prototype['weeklySchedule'] = undefined;






export default SameDayShippingCutOffTimes;

