/**
 * Inventory API
 * The Inventory API is used to create and manage inventory, and then to publish and manage this inventory on an eBay marketplace. There are also methods in this API that will convert eligible, active eBay listings into the Inventory API model.
 *
 * The version of the OpenAPI document: 1.18.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import InventoryItemWithSkuLocaleGroupid from './InventoryItemWithSkuLocaleGroupid';

/**
 * The InventoryItems model module.
 * @module model/InventoryItems
 * @version 1.18.4
 */
class InventoryItems {
    /**
     * Constructs a new <code>InventoryItems</code>.
     * This type is used by the base response payload of &lt;strong&gt;getInventoryItems&lt;/strong&gt; call. 
     * @alias module:model/InventoryItems
     */
    constructor() { 
        
        InventoryItems.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>InventoryItems</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InventoryItems} obj Optional instance to populate.
     * @return {module:model/InventoryItems} The populated <code>InventoryItems</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InventoryItems();

            if (data.hasOwnProperty('href')) {
                obj['href'] = ApiClient.convertToType(data['href'], 'String');
            }
            if (data.hasOwnProperty('inventoryItems')) {
                obj['inventoryItems'] = ApiClient.convertToType(data['inventoryItems'], [InventoryItemWithSkuLocaleGroupid]);
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('next')) {
                obj['next'] = ApiClient.convertToType(data['next'], 'String');
            }
            if (data.hasOwnProperty('prev')) {
                obj['prev'] = ApiClient.convertToType(data['prev'], 'String');
            }
            if (data.hasOwnProperty('size')) {
                obj['size'] = ApiClient.convertToType(data['size'], 'Number');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InventoryItems</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InventoryItems</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['href'] && !(typeof data['href'] === 'string' || data['href'] instanceof String)) {
            throw new Error("Expected the field `href` to be a primitive type in the JSON string but got " + data['href']);
        }
        if (data['inventoryItems']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['inventoryItems'])) {
                throw new Error("Expected the field `inventoryItems` to be an array in the JSON data but got " + data['inventoryItems']);
            }
            // validate the optional field `inventoryItems` (array)
            for (const item of data['inventoryItems']) {
                InventoryItemWithSkuLocaleGroupid.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['next'] && !(typeof data['next'] === 'string' || data['next'] instanceof String)) {
            throw new Error("Expected the field `next` to be a primitive type in the JSON string but got " + data['next']);
        }
        // ensure the json data is a string
        if (data['prev'] && !(typeof data['prev'] === 'string' || data['prev'] instanceof String)) {
            throw new Error("Expected the field `prev` to be a primitive type in the JSON string but got " + data['prev']);
        }

        return true;
    }


}



/**
 * This is the URL to the current page of inventory items.
 * @member {String} href
 */
InventoryItems.prototype['href'] = undefined;

/**
 * This container is an array of one or more inventory items, with detailed information on each inventory item.
 * @member {Array.<module:model/InventoryItemWithSkuLocaleGroupid>} inventoryItems
 */
InventoryItems.prototype['inventoryItems'] = undefined;

/**
 * This integer value is the number of inventory items that will be displayed on each results page.
 * @member {Number} limit
 */
InventoryItems.prototype['limit'] = undefined;

/**
 * This is the URL to the next page of inventory items. This field will only be returned if there are additional inventory items to view.
 * @member {String} next
 */
InventoryItems.prototype['next'] = undefined;

/**
 * This is the URL to the previous page of inventory items. This field will only be returned if there are previous inventory items to view.
 * @member {String} prev
 */
InventoryItems.prototype['prev'] = undefined;

/**
 * This integer value indicates the total number of pages of results that are available. This number will depend on the total number of inventory items available for viewing, and on the <strong>limit</strong> value.
 * @member {Number} size
 */
InventoryItems.prototype['size'] = undefined;

/**
 * This integer value is the total number of inventory items that exist for the seller's account. Based on this number and on the <strong>limit</strong> value, the seller may have to toggle through multiple pages to view all inventory items.
 * @member {Number} total
 */
InventoryItems.prototype['total'] = undefined;






export default InventoryItems;

