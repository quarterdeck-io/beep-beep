/**
 * Inventory API
 * The Inventory API is used to create and manage inventory, and then to publish and manage this inventory on an eBay marketplace. There are also methods in this API that will convert eligible, active eBay listings into the Inventory API model.
 *
 * The version of the OpenAPI document: 1.18.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Error from './Error';
import InventoryItemWithSkuLocaleGroupKeys from './InventoryItemWithSkuLocaleGroupKeys';

/**
 * The GetInventoryItemResponse model module.
 * @module model/GetInventoryItemResponse
 * @version 1.18.4
 */
class GetInventoryItemResponse {
    /**
     * Constructs a new <code>GetInventoryItemResponse</code>.
     * This type is used by the response of the &lt;strong&gt;bulkGetInventoryItem&lt;/strong&gt; method to give the status of each inventory item record that the user tried to retrieve.
     * @alias module:model/GetInventoryItemResponse
     */
    constructor() { 
        
        GetInventoryItemResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GetInventoryItemResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetInventoryItemResponse} obj Optional instance to populate.
     * @return {module:model/GetInventoryItemResponse} The populated <code>GetInventoryItemResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetInventoryItemResponse();

            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [Error]);
            }
            if (data.hasOwnProperty('inventoryItem')) {
                obj['inventoryItem'] = InventoryItemWithSkuLocaleGroupKeys.constructFromObject(data['inventoryItem']);
            }
            if (data.hasOwnProperty('sku')) {
                obj['sku'] = ApiClient.convertToType(data['sku'], 'String');
            }
            if (data.hasOwnProperty('statusCode')) {
                obj['statusCode'] = ApiClient.convertToType(data['statusCode'], 'Number');
            }
            if (data.hasOwnProperty('warnings')) {
                obj['warnings'] = ApiClient.convertToType(data['warnings'], [Error]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetInventoryItemResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetInventoryItemResponse</code>.
     */
    static validateJSON(data) {
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                Error.validateJSON(item);
            };
        }
        // validate the optional field `inventoryItem`
        if (data['inventoryItem']) { // data not null
          InventoryItemWithSkuLocaleGroupKeys.validateJSON(data['inventoryItem']);
        }
        // ensure the json data is a string
        if (data['sku'] && !(typeof data['sku'] === 'string' || data['sku'] instanceof String)) {
            throw new Error("Expected the field `sku` to be a primitive type in the JSON string but got " + data['sku']);
        }
        if (data['warnings']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['warnings'])) {
                throw new Error("Expected the field `warnings` to be an array in the JSON data but got " + data['warnings']);
            }
            // validate the optional field `warnings` (array)
            for (const item of data['warnings']) {
                Error.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * This container will be returned if there were one or more errors associated with retrieving the inventory item record.
 * @member {Array.<module:model/Error>} errors
 */
GetInventoryItemResponse.prototype['errors'] = undefined;

/**
 * @member {module:model/InventoryItemWithSkuLocaleGroupKeys} inventoryItem
 */
GetInventoryItemResponse.prototype['inventoryItem'] = undefined;

/**
 * The seller-defined Stock-Keeping Unit (SKU) of the inventory item. The seller should have a unique SKU value for every product that they sell.
 * @member {String} sku
 */
GetInventoryItemResponse.prototype['sku'] = undefined;

/**
 * The HTTP status code returned in this field indicates the success or failure of retrieving the inventory item record for the inventory item specified in the <strong>sku</strong> field. See the <strong>HTTP status codes</strong> table to see which each status code indicates.
 * @member {Number} statusCode
 */
GetInventoryItemResponse.prototype['statusCode'] = undefined;

/**
 * This container will be returned if there were one or more warnings associated with retrieving the inventory item record.
 * @member {Array.<module:model/Error>} warnings
 */
GetInventoryItemResponse.prototype['warnings'] = undefined;






export default GetInventoryItemResponse;

