/**
 * Inventory API
 * The Inventory API is used to create and manage inventory, and then to publish and manage this inventory on an eBay marketplace. There are also methods in this API that will convert eligible, active eBay listings into the Inventory API model.
 *
 * The version of the OpenAPI document: 1.18.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Interval from './Interval';

/**
 * The OperatingHours model module.
 * @module model/OperatingHours
 * @version 1.18.4
 */
class OperatingHours {
    /**
     * Constructs a new <code>OperatingHours</code>.
     * This type is used to express the regular operating hours of a merchant&#39;s store or fulfillment center during the days of the week.
     * @alias module:model/OperatingHours
     */
    constructor() { 
        
        OperatingHours.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OperatingHours</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OperatingHours} obj Optional instance to populate.
     * @return {module:model/OperatingHours} The populated <code>OperatingHours</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OperatingHours();

            if (data.hasOwnProperty('dayOfWeekEnum')) {
                obj['dayOfWeekEnum'] = ApiClient.convertToType(data['dayOfWeekEnum'], 'String');
            }
            if (data.hasOwnProperty('intervals')) {
                obj['intervals'] = ApiClient.convertToType(data['intervals'], [Interval]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OperatingHours</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OperatingHours</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['dayOfWeekEnum'] && !(typeof data['dayOfWeekEnum'] === 'string' || data['dayOfWeekEnum'] instanceof String)) {
            throw new Error("Expected the field `dayOfWeekEnum` to be a primitive type in the JSON string but got " + data['dayOfWeekEnum']);
        }
        if (data['intervals']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['intervals'])) {
                throw new Error("Expected the field `intervals` to be an array in the JSON data but got " + data['intervals']);
            }
            // validate the optional field `intervals` (array)
            for (const item of data['intervals']) {
                Interval.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * A <strong>dayOfWeekEnum</strong> value is required for each day of the week that the store location has regular operating hours. <br><br>This field is returned if operating hours are defined for the store location. For implementation help, refer to <a href='https://developer.ebay.com/api-docs/sell/inventory/types/api:DayOfWeekEnum'>eBay API documentation</a>
 * @member {String} dayOfWeekEnum
 */
OperatingHours.prototype['dayOfWeekEnum'] = undefined;

/**
 * This container is used to define the opening and closing times of a store location's working day (defined in the <strong>dayOfWeekEnum</strong> field). An <strong>intervals</strong> container is needed for each day of the week that the store location is open. If a store location closes for lunch (or any other period during the day) and then reopens, multiple <strong>open</strong> and <strong>close</strong> pairs are needed <br><br>This container is returned if operating hours are defined for the store location.
 * @member {Array.<module:model/Interval>} intervals
 */
OperatingHours.prototype['intervals'] = undefined;






export default OperatingHours;

