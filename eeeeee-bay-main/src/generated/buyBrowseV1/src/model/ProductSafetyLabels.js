/**
 * Browse API
 * The Browse API has the following resources:<ul><li><b>item_summary:</b><br>Allows shoppers to search for specific items by keyword, GTIN, category, charity, product, image, or item aspects and refine the results by using filters, such as aspects, compatibility, and fields values, or UI parameters.</li><li><b>item:</b><br>Allows shoppers to retrieve the details of a specific item or all items in an item group, which is an item with variations such as color and size and check if a product is compatible with the specified item, such as if a specific car is compatible with a specific part.<br><br>This resource also provides a bridge between the eBay legacy APIs, such as the <a href=\"/api-docs/user-guides/static/finding-user-guide-landing.html\" target=\"_blank\">Finding</b>, and the RESTful APIs, which use different formats for the item IDs.</li></ul>The <b>item_summary</b>, <b>search_by_image</b>, and <b>item</b> resource calls require an <a href=\"/api-docs/static/oauth-client-credentials-grant.html\" target=\"_blank\">Application access token</a>.
 *
 * The version of the OpenAPI document: v1.20.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ProductSafetyLabelPictogram from './ProductSafetyLabelPictogram';
import ProductSafetyLabelStatement from './ProductSafetyLabelStatement';

/**
 * The ProductSafetyLabels model module.
 * @module model/ProductSafetyLabels
 * @version v1.20.4
 */
class ProductSafetyLabels {
    /**
     * Constructs a new <code>ProductSafetyLabels</code>.
     * This type contains seller provided product safety pictograms and statements for the listing.
     * @alias module:model/ProductSafetyLabels
     */
    constructor() { 
        
        ProductSafetyLabels.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ProductSafetyLabels</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProductSafetyLabels} obj Optional instance to populate.
     * @return {module:model/ProductSafetyLabels} The populated <code>ProductSafetyLabels</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProductSafetyLabels();

            if (data.hasOwnProperty('pictograms')) {
                obj['pictograms'] = ApiClient.convertToType(data['pictograms'], [ProductSafetyLabelPictogram]);
            }
            if (data.hasOwnProperty('statements')) {
                obj['statements'] = ApiClient.convertToType(data['statements'], [ProductSafetyLabelStatement]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProductSafetyLabels</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProductSafetyLabels</code>.
     */
    static validateJSON(data) {
        if (data['pictograms']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['pictograms'])) {
                throw new Error("Expected the field `pictograms` to be an array in the JSON data but got " + data['pictograms']);
            }
            // validate the optional field `pictograms` (array)
            for (const item of data['pictograms']) {
                ProductSafetyLabelPictogram.validateJSON(item);
            };
        }
        if (data['statements']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['statements'])) {
                throw new Error("Expected the field `statements` to be an array in the JSON data but got " + data['statements']);
            }
            // validate the optional field `statements` (array)
            for (const item of data['statements']) {
                ProductSafetyLabelStatement.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * An array of seller provided comma-separated string values that provides identifier, URL, and description for one or more pictograms associated with the listing.
 * @member {Array.<module:model/ProductSafetyLabelPictogram>} pictograms
 */
ProductSafetyLabels.prototype['pictograms'] = undefined;

/**
 * An array of seller provided comma-separated string values that provide identifier and description for one or more product safety statements associated with the listing.
 * @member {Array.<module:model/ProductSafetyLabelStatement>} statements
 */
ProductSafetyLabels.prototype['statements'] = undefined;






export default ProductSafetyLabels;

